.venv
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.git
.gitignore
# Local pose_models/ omitted from context; Dockerfiles fetch weights at build time.
pose_models

# Large media / artifacts
*.MOV
*.mp4
**/*.MOV
**/*.mp4

# Optional local ONNX at repo root — not used by YOLO .pt server path
*.onnx
